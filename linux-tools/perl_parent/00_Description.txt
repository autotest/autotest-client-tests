#########################################################################################
#Establish an ISA(has-a) relationship with base classes at compile time.
#
#parent allows you to both load one or more modules, while setting up
#inheritance from those modules at the same time.
#########################################################################################
running perl tests 
cd /opt/fiv/ltp/testcases/fivextra/perl-parent
run the test_perl_parent.sh script
